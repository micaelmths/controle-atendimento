{
  "routes": [
    {
      "method": "GET",
      "path": "/pacientes",
      "handler": "pacientes.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pacientes/count",
      "handler": "pacientes.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pacientes/:id",
      "handler": "pacientes.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pacientes",
      "handler": "pacientes.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pacientes/:id",
      "handler": "pacientes.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pacientes/:id",
      "handler": "pacientes.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
